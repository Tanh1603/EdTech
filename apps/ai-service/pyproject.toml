[project]
name = "ai-service"
version = "0.1.0"
description = "Internal AI orchestration, RAG, and worker service for EdTech"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn>=0.47.0",
]
